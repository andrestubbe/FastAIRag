# FastAIRag Compilation Guide

## Requirements
* JDK 17+
* Apache Maven 3.8+

## Build Command
```powershell
mvn clean install -DskipTests
```
This builds `FastAIRag-0.1.0.jar` and installs it to your local Maven repository (`~/.m2/repository`).
